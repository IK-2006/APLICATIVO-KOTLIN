<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppAula"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- Login como tela inicial -->
        <activity
            android:name="br.upf.ads.mercadoestoque.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="br.upf.ads.mercadoestoque.MainActivity"
            android:exported="false" />

        <activity
            android:name="br.upf.ads.mercadoestoque.CadastroProdutoActivity"
            android:exported="false"
            android:parentActivityName="br.upf.ads.mercadoestoque.MainActivity" />

        <activity
            android:name="br.upf.ads.mercadoestoque.ListaProdutosActivity"
            android:exported="false"
            android:parentActivityName="br.upf.ads.mercadoestoque.MainActivity" />

    </application>

</manifest>